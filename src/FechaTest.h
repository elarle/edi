#ifndef FECHATEST_H_
#define FECHATEST_H_

#include "Fecha.h"

/*
 * Creamos una fecha por defecto y probamos los setters y getters
 * de día, mes y año. Creamos una fecha con el constructor parametrizado
 * y comprobamos que guarde los datos correctos. Probamos el constructor
 * de copia y verificamos que los datos se dupliquen bien.
 * Mostramos la fecha por pantalla para revisión visual.
 */
void pruebaFecha();

#endif /* FECHATEST_H_ */
